<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet para backend Node.js / MongoDB -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Verificar conexión -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Reconocimiento de voz -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Biometría -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:label="TheFitness App"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.TheFtiness_App">

        <!-- Pantalla inicial (Huella) -->
        <activity
            android:name=".ui.BiometricActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Pantalla principal -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Registro por voz -->
        <activity
            android:name=".ui.VoiceTrainingActivity"
            android:exported="false" />

        <!-- Gráfica de progreso -->
        <activity
            android:name=".ui.ProgressActivity"
            android:exported="false" />

    </application>

</manifest>